# Coril BL Optimizer — Streamlit Community Cloud
#
# IMPORTANTE: este proyecto usa Python 3.12 (ver archivo runtime.txt).
# Motivo: Python 3.14 obliga a instalar streamlit 1.61.0, que tiene un bug
# de arranque (GZipResponder / thread_minimum_size). En Python 3.12 se puede
# usar una versión estable de streamlit sin ese bug.
streamlit==1.50.0
numpy>=1.26,<2.2
pandas>=2.2,<2.3
scipy>=1.13
scikit-learn>=1.5
yfinance>=0.2.50
plotly>=5.24
